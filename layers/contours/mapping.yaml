tables:
  # etldoc: imposm3 -> osm_contour
  contour:
    # requires:
    #   tables:
    #     - osm_contour
    type: linestring
    columns:
    # - name: osm_id
    #   type: id
      # from_member: true
    - name: geometry
      type: geometry
    - name: ele
      key: ele
      type: integer
    mapping:
      contour:
        - elevation
